apply plugin: 'com.android.library'

android {
    compileSdkVersion 34
    buildToolsVersion "33.0.0"

    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"
    }
}

dependencies {
    implementation 'com.facebook.react:react-native:+'
    implementation 'com.portmone.ecomsdk:ecomsdk:3.0.11'
}
